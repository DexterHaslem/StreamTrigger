<Window x:Class="StreamTrigger.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:StreamTrigger"
        mc:Ignorable="d"
        Title="StreamTrigger" Height="350" Width="525">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto" />
            <ColumnDefinition />
            <ColumnDefinition Width="Auto" />
        </Grid.ColumnDefinitions>

        <Label Grid.Row="0" Grid.Column="0">Stream name:</Label>
        <TextBox Grid.Row="0" Grid.Column="1" Text="{Binding StreamName}" HorizontalAlignment="Stretch" />


        <Label Grid.Row="1" Grid.Column="0">File To Execute:</Label>
        <TextBox Grid.Row="1" Grid.Column="1" Text="{Binding FileToExecute}" HorizontalAlignment="Stretch"/>
        <Button Click="OnClick">...</Button>

        <Label Grid.Row="2" Grid.ColumnSpan="2" Content="{Binding StatusText}" />
    </Grid>
</Window>
